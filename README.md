# Парсер раздач PokerOK на Python

HandParser — это консольный Python-скрипт для анализа истории раздач (hand history) покер-рума PokerOK.  

Скрипт:

- читает текстовые файлы с раздачами,
- парсит структуру каждой раздачи,
- находит руки героя (по нику),
- считает базовую покерную статистику (VPIP, PFR, 3-bet, AF, C-bet, WTSD, W$SD, BB/100),
- выводит сводку в консоль и при желании сохраняет результат в **CSV** или **JSON**.

Цель проекта — иметь простой, открытый и настраиваемый инструмент для анализа игры на PokerOK, без тяжёлых зависимостей и сложных установок.

---

## 🔧 Возможности

Основной функционал `handparser.py`:

- 📂 Обработка **одного файла** с историей раздач или **целой папки** с HH:
  - поддержка рекурсивного обхода каталогов;
- 🧑‍💻 Анализ по **герою** (ник указывается параметром `--hero`);
- 📊 Расчёт ключевых статов:
  - **Hands with hero** — количество рук с участием героя;
  - **Total profit** — итоговый профит в деньгах;
  - **BB/100** — винрейт в больших блайндах;
  - **VPIP** — добровольное участие в банке;
  - **PFR** — префлоп агрессия;
  - **3-bet** — частота 3-бета и количество спотов;
  - **AF** — постфлоп коэффициент агрессии;
  - **C-bet flop** — контбет на флопе и количество спотов;
  - **WTSD** — Went To Showdown;
  - **W$SD** — Won $ at Showdown;
- 🪜 Разбивка статистики по лимитам:
  - автоматическое определение лимита вида `$0.05/$0.10` → `NL10`;
  - отдельная статистика по каждому лимиту;
- 🧾 Экспорт результатов:
  - человекочитаемый **summary** в консоль;
  - **CSV**-файл (статы по лимитам);
  - **JSON** (глобальные статы + по лимитам);
- 🧪 Встроенный **self-test** — пример нескольких раздач для проверки работы парсера.

---

## 📦 Требования

- **Python 3.9+**
- Только стандартная библиотека:
  - `argparse`, `csv`, `json`, `logging`, `re`, `dataclasses`, `datetime`, `pathlib`, `typing`, `sys`.
  
---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/PokerOk-H2/Hand-Parser.git
cd Hand-Parser
```

### 2. Проверка версии Python

```bash
python --version
# или
python3 --version
```

Убедитесь, что версия Python не ниже **3.9**.

### 3. Первый запуск (self-test)

Скрипт содержит встроенный тестовый пример раздач.
Это удобно, чтобы убедиться, что всё работает:

```bash
python handparser.py --input . --hero HeroNick --self-test
```

Параметр `--input` в режиме `--self-test` не важен — будет использован встроенный текст.

---

## 🧠 Формат входных данных

Скрипт рассчитан на текстовые HH-файлы PokerOK / GGPokerOK или похожего формата.

**Предположения:**

* Каждая раздача начинается строкой с `Hand #...`.
* В заголовке указывается:

  * ID раздачи,
  * тип игры (например, Hold'em No Limit),
  * лимит (например, `$0.05/$0.10`),
  * дата и время.
* Далее идут:

  * информация о столе (6-max, 9-max и т.п.),
  * список игроков (`Seat 1: Name ($10 in chips)`),
  * блоки `*** HOLE CARDS ***`, `*** FLOP ***`, `*** TURN ***`, `*** RIVER ***`, `*** SHOW DOWN ***`, `*** SUMMARY ***`,
  * действия игроков на улицах,
  * строки с `Total pot`, `collected`, `won` и т.д.

⚠️ Формат HH в PokerOK может немного отличаться, поэтому регулярные выражения в коде можно будет при необходимости адаптировать под реальные файлы.

---

## 🛠 Использование

### Базовый пример

Анализ одного файла с раздачами:

```bash
python handparser.py --input hands_2025_01_01.txt --hero MyNick
```

### Анализ папки с HH-файлами

```bash
python handparser.py --input ./hands/ --hero MyNick
```

Рекурсивный обход всех подпапок:

```bash
python handparser.py --input ./hands/ --hero MyNick --recursive
```

### Фильтрация по лимитам

Можно анализировать не все лимиты, а только нужные, например **NL10** и **NL25**:

```bash
python handparser.py --input ./hands/ --hero MyNick --stakes "NL10,NL25"
```

### Минимальное количество рук на лимит

По умолчанию в таблицу по лимитам попадают только те лимиты, где сыграно хотя бы 30 рук.
Можно изменить порог, например до 50:

```bash
python handparser.py --input ./hands/ --hero MyNick --min-hands 50
```

---

## 📤 Экспорт результатов

### Вывод только в консоль (по умолчанию)

```bash
python handparser.py --input ./hands/ --hero MyNick
```

### Экспорт в CSV

```bash
python handparser.py --input ./hands/ --hero MyNick --out stats.csv --out-format csv
```

В результате будет создан файл `stats.csv` со строками вида:

* лимит (NL10, NL25 и т.д.),
* количество рук,
* VPIP, PFR, 3-bet, BB/100,
* профит и другие метрики.

### Экспорт в JSON

```bash
python handparser.py --input ./hands/ --hero MyNick --out stats.json --out-format json
```

Формат JSON:

```json
{
  "global": {
    "hands_total": ...,
    "hands_with_hero": ...,
    "vpip": ...,
    "pfr": ...,
    "threebet": ...,
    "threebet_spots": ...,
    "af": ...,
    "cbet_flop": ...,
    "cbet_flop_spots": ...,
    "wtsd": ...,
    "wsd": ...,
    "total_profit": ...,
    "bb_per_100": ...
  },
  "by_stakes": {
    "NL10": {
      "...": "..."
    },
    "NL25": {
      "...": "..."
    }
  }
}
```

---

## 🔍 Параметры командной строки

Кратко:

```bash
python handparser.py --input PATH --hero HERO_NAME [опции]
```

**Обязательные параметры:**

* `-i, --input`
  Путь к файлу или директории с hand history.

* `-H, --hero`
  Никнейм героя (игрока), по которому считается статистика.

**Основные опции:**

* `-r, --recursive`
  Рекурсивный поиск файлов в подкаталогах при указании директории.

* `-s, --stakes`
  Список лимитов через запятую (например, `"NL10,NL25"`), остальные лимиты будут проигнорированы.

* `--min-hands`
  Минимальное число рук для отображения лимита в таблице по лимитам (по умолчанию: `30`).

* `-o, --out`
  Имя файла для экспорта (`.csv` или `.json` в зависимости от формата).

* `--out-format`
  Формат вывода:

  * `summary` — только человекочитаемое резюме в консоль (по умолчанию),
  * `csv` — экспорт в CSV,
  * `json` — экспорт в JSON.

* `--encoding`
  Принудительная кодировка для чтения файлов.
  По умолчанию: сначала `utf-8`, затем `cp1251`.

* `--debug`
  Включает более подробные сообщения логирования (полезно при отладке формата HH).

* `--self-test`
  Запускает встроенный тест с примерными раздачами и завершает работу.

---

## 📊 Какие статы считаются

Для героя рассчитываются:

* **Hands with hero** — количество рук, где герой сидит за столом;
* **Total profit** — суммарный профит (выигрыш–проигрыш, с учётом вложенных фишек и собранных банков);
* **BB/100** — винрейт в больших блайндах на 100 рук (для рук, где удалось распознать размер BB);
* **VPIP** — % рук, в которых герой добровольно вложил деньги на префлопе (call/raise, без учёта обязательных блайндов);
* **PFR** — % рук, в которых герой проявил агрессию на префлопе (bet/raise/3-bet);
* **3-bet** — % 3-бетов от количества спотов, где у героя была возможность 3-бетить;
* **AF (Aggression Factor)** — (bets + raises) / calls по постфлопу;
* **C-bet flop** — % контбетов на флопе от количества спотов, где герой был префлоп-агрессором и увидел флоп;
* **WTSD (Went To Showdown)** — % рук, в которых герой дошёл до шоудауна;
* **W$SD (Won $ at Showdown)** — % выигранных шоудаунов от числа шоудаунов.

Все эти показатели также считаются **по каждому лимиту** (NL2, NL5, NL10 и т.д.), если хватает выборки рук.

---

## ⚠️ Ограничения и оговорки

* Текущая версия парсера использует **обобщённые регулярные выражения**, ориентировочно подходящие под формат PokerOK / GGPokerOK.
* Возможны расхождения с реальными HH-файлами:

  * некоторые строки могут не распознаться,
  * часть раздач может быть пропущена,
  * не все типы действий/игр могут быть поддержаны.
* Скрипт изначально оптимизирован под **кэш-игры** (NLH / PLO), но базовая логика может работать и для других форматов при корректном HH.

Если у вас есть реальные HH-файлы, вы можете:

* адаптировать регулярные выражения в коде (`HEADER_REGEX`, `SEAT_REGEX`, `DEALT_REGEX`, `BOARD_*`, `parse_action_line`);
* открыть Issue или Pull Request с предложениями по улучшению.

---

## 🤝 Вклад и доработка

Любые предложения по улучшению приветствуются:

* более точная поддержка формата PokerOK;
* добавление:

  * новых статов (например, 4-bet, fold to 3-bet, steal и т.д.),
  * визуализаций (графики winrate, распределения профита по времени),
  * поддержки других румов.

Шаги:

1. Форкните репозиторий.
2. Создайте новую ветку: `feature/my-improvement`.
3. Внесите изменения.
4. Откройте Pull Request.

---

## 📄 Лицензия

Проект распространяется по лицензии **MIT**. Вы можете свободно использовать, изменять и распространять этот код, при условии сохранения уведомления об авторских правах и текста лицензии. Полный текст лицензии доступен в файле [`LICENSE`](./LICENSE) (если присутствует в репозитории) или может быть добавлен при инициализации проекта.
